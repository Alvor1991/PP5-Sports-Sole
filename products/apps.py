from django.apps import AppConfig


class ProfilesConfig(AppConfig):
    """
    Configuration for the profiles application, which handles user profile
    data and related functionalities within the project.
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'products'
